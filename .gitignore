node_modules
dist
storybook-static
.superpowers
.next

# Generated design tokens (rebuilt by `npm run generate:tokens`)
src/styles/tokens.css
src/styles/tokens.ts
src/styles/tokens.json

# Environment files
.env
.env.*
!.env.example

# OS files
.DS_Store

# Build artifacts (OpenSSF Scorecard: Binary-Artifacts)
tsconfig.tsbuildinfo

# Local project state (not for version control)
conductor-state.json
.playwright-mcp/
*.png

# Open Design bridge output (regenerated by `npm run generate:design-md`)
open-design-export/
